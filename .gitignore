node_modules
dist
.DS_Store
*.local
*.tsbuildinfo

# Real WhatsApp/Supabase secrets — never commit. server/.env.example is the
# committed template with no real values in it.
server/.env

# Supabase CLI local scratch state (link target, cli version marker) — not source
supabase/.temp/
dev-dist
